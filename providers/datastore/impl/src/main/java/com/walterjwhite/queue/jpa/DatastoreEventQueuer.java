package com.walterjwhite.queue.jpa;

@Deprecated
public class DatastoreEventQueuer {
  //    protected boolean supportsJPAAction(
  //            final JPAActionType jpaActionType, final SubscribeTo subscribeTo) {
  //        return Arrays.stream(subscribeTo.jpaAction())
  //                .filter(supportedJPAAction -> jpaActionType.equals(supportedJPAAction))
  //                .count()
  //                > 0;
  //    }
  //
  //
  //    return supportsJPAAction(jpaActionType, subscribeTo);
}
